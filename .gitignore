# Python
__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Env / secrets
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store

# Build / dist
dist/
build/
*.whl

# Node (miniapp, M3+)
node_modules/

# Content pipeline outputs (large, regenerable)
pipeline/packs/*.json
pipeline/packs/*.tar.gz
# Review samples go out to a reviewer and come back filled in; the filled-in
# copy is evidence for M1 and belongs wherever that is kept, not in git.
pipeline/packs/*.csv
